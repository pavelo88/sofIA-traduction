
{
  "entities": {
    "ChatMessage": {
      "title": "ChatMessage",
      "description": "Un mensaje individual en la conversación con Kitten.",
      "type": "object",
      "properties": {
        "role": { "type": "string", "enum": ["user", "model"] },
        "content": { "type": "string" },
        "timestamp": { "type": "string" },
        "user_email": { "type": "string" }
      },
      "required": ["role", "content", "timestamp", "user_email"]
    },
    "UserProgress": {
      "title": "UserProgress",
      "description": "Estadísticas de progreso del usuario.",
      "type": "object",
      "properties": {
        "accuracy_percentage": { "type": "number" },
        "user_email": { "type": "string" },
        "last_grade": { "type": "string" },
        "updated_at": { "type": "string" }
      },
      "required": ["accuracy_percentage", "user_email"]
    },
    "UserProfile": {
      "title": "UserProfile",
      "description": "Configuración y perfil extendido del usuario.",
      "type": "object",
      "properties": {
        "nativeLanguage": { "type": "string" },
        "targetLanguage": { "type": "string" },
        "aiEngineMode": { "type": "string" },
        "updated_at": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/chat_history/{messageId}": {
      "schema": "ChatMessage",
      "description": "Colección de historial de chat para memoria de la IA."
    },
    "/user_progress/{userId}": {
      "schema": "UserProgress",
      "description": "Información de progreso y nivel del usuario."
    },
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Datos de configuración de perfil de usuario."
    }
  }
}
